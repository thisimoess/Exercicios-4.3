#include <stdio.h>
#include <string.h>

int main() {
    char nome[100], sexo;
    int idade;
    printf("Digite o nome: ");
    scanf("%s", nome);
    printf("Digite o sexo (M/F): ");
    scanf(" %c", &sexo);
    printf("Digite a idade: ");
    scanf("%d", &idade);
    if (sexo == 'M' && idade < 18) {
        printf("%s Não aceito\n", nome);
    } else {
        printf("Aceito\n");
    }
    return 0;
}
